{
  "name": "kptnuncook",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "mkdir -p functions && cp shared/* functions/ && mkdir -p public && cp shared/* public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.7.0",
    "ejs": "^3.1.10"
  },
  "description": ""
}